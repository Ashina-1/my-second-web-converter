// Copy this file to `firebase-config.js` and fill in your real values.
// Keep `firebase-config.js` out of source control (it's in .gitignore).

// This example file shows how to provide the firebaseConfig object.
// Copy this file to `firebase-config.js` and replace the placeholder
// values with your project's values. Do NOT import SDK modules here
// when using the static ES module pages — keep initializer logic in
// `firebase-init.js` instead.

export const firebaseConfig = {
  apiKey: "REPLACE_WITH_YOUR_API_KEY",
  authDomain: "REPLACE_WITH_YOUR_AUTH_DOMAIN",
  projectId: "REPLACE_WITH_YOUR_PROJECT_ID",
  storageBucket: "REPLACE_WITH_YOUR_STORAGE_BUCKET",
  messagingSenderId: "REPLACE_WITH_YOUR_MESSAGING_SENDER_ID",
  appId: "REPLACE_WITH_YOUR_APP_ID",
  measurementId: "REPLACE_WITH_YOUR_MEASUREMENT_ID",
};

// If you prefer to initialize Firebase inside this file (not recommended
// for this repo), you could import the SDKs and call initializeApp here.
